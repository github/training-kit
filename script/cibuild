#!/bin/sh

set -e

# setup environment
export PATH="/usr/share/rbenv/shims:$PATH"

# write some debug info
ruby -v
echo "hostname: $(hostname)"
echo "pwd: $(pwd)"
git --version

script/bootstrap

cd /_site
find . -type f -name "*.html" >> ./script/list-of-files.txt
cd ..

bundle exec rake test
