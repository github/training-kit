{% if svg_path %}
<svg version="1.1"
	width="100%"
	height="100%"
	data-path="{{ svg_path }}"
	id="{{ svg_path | remove: '/' | remove: '.' | remove: '..' }}"
	viewBox="0 0 100 100"></svg>
{% endif %}
